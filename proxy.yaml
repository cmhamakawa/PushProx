apiVersion: apps/v1
kind: Deployment
metadata:
  name: pushproxy-deployment
  labels:
    app: pushproxy-deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: pushproxy-deployment
  template:
    metadata:
      labels:
        app: pushproxy-deployment
    spec:
      containers:
      - name: pushproxy-deployment
        image: christineacr.azurecr.io/pushprox_proxy
        ports:
          - containerPort: 8080
---
apiVersion: v1
kind: Service
metadata:
  name: proxy
spec:
  selector:
    app: pushproxy-deployment
  ports:
    - port: 8080
  type: LoadBalancer